function main[main](var request) {
	try {
		var func = dlfunc(null, "ap_rwrite", "vpip");
		var buf = new ['ByteBuffer'];
		buf =: "hello, ap_rwrite!\n";
		func(buf, elements(buf), request);
	} catch(e) { say(e.message); }
	return;
}
